using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UtilityTime : MonoBehaviour
{
    /*
     *  float timeSet = n;
     *  bool timeCheck = true;
     *  Update: 
     *  timeSet -= Time.deltaTime;
     *  if(timeSet < 0 && timeCheck){
                timeCheck = false;
        }
     */
}
