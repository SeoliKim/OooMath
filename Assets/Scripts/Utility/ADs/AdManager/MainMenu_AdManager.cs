using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainMenu_AdManager : AdManager {
    // Start is called before the first frame update
    void Start()
    {
        SetRewardedAdsButton();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
