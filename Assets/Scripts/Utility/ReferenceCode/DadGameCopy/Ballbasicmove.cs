using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ballbasicmove : MonoBehaviour
{
    
    /*
    public float inputForce = 1;
    public float vMax = 0.2f;
    public float cutOffSpeed = 5;

    public float InputForceFactor = 1f;
    public float VMaxFactor = 1f;

    private Rigidbody _rig;

    private void Awake() {
        _rig = GetComponent<Rigidbody>();
    }

    //Helper?
    public virtual void Move(Vector3 accel) {
        _rig.AddForce(accel, ForceMode.Force);
    }

    public virtual void MoveTo(Vector3 pos) {
        pos.y = transform.position.y;
        var accel = (pos - transform.position).normalized;

        Move(accel);
    }

    public virtual void Pursue(Rigidbody target) {
        throw new System.NotImplementedException();
    }

    public virtual void Evade(Rigidbody target) {
        throw new System.NotImplementedException();
    }

    public virtual void Restore() {
        InputForceFactor = 1;
        VMaxFactor = 1;
    }
    */
}

